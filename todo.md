Header
We noticed references to additional assets such as styles.css, tokens.css, and script.js. Could you please confirm whether the EDS base theme is being included?
A sticky header implementation is required, as the eds-header is expected to remain fixed during scrolling.
The secondary navigation includes a custom eds-container class, which differs from the standard EDS structure.
The DOM structure for the logo and secondary navigation links does not align with the EDS implementation.
The logo has been built using multiple image assets, whereas the Figma design specifies a single SVG logo.
The logo is placed within a primary-bar section, which is not part of the standard EDS architecture.
The blue bar containing page navigation links appears to deviate from the expected EDS structure.
EDS provides a separate secondary navigation experience for mobile devices; the current implementation differs from this approach.
Landing Hero Banner
The landing page hero section titled“About Vanddmyo” (the blue-background section directly beneath the header) is currently missing.
2 Column Content Block
The structure aligns with the EDS implementation.
Card Component
The three-column card section follows the EDS structure; however, other card implementations do not. Additionally, the component order appears reversed, with card-body-footer preceding card-img.
The section titled“Your doctor is the best person…” could be implemented either as a Content Block or a Card component. While using a Card is acceptable, the current structure deviates from the EDS Card structure.
ISI
The structure does not align with EDS.
Footer
The structure does not align with EDS.
