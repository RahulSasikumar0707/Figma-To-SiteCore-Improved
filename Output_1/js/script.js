// Cover page — no interactive behaviour required.
// Appearance changes must be class toggles only (no element.style writes).
document.addEventListener('DOMContentLoaded', function () {
  document.body.classList.add('cover-ready');
});
